interface:
  display_name: "Mind Forge Plan"
  short_description: "Plan and reshape articles through iterative prompts"
  default_prompt: "Use $mf-plan to plan or replan this article from its current repository state."
