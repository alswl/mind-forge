<!-- Generated by `mf --generate-manual`; do not edit directly. -->

# Command-Line Help for `mf`

This document contains the help content for the `mf` command-line program.

**Command Overview:**

* [`mf`↴](#mf)
* [`mf init`↴](#mf-init)
* [`mf project`↴](#mf-project)
* [`mf project new`↴](#mf-project-new)
* [`mf project list`↴](#mf-project-list)
* [`mf project archive`↴](#mf-project-archive)
* [`mf project lint`↴](#mf-project-lint)
* [`mf project index`↴](#mf-project-index)
* [`mf project show`↴](#mf-project-show)
* [`mf project update`↴](#mf-project-update)
* [`mf project import`↴](#mf-project-import)
* [`mf project rename`↴](#mf-project-rename)
* [`mf project remove`↴](#mf-project-remove)
* [`mf article`↴](#mf-article)
* [`mf article new`↴](#mf-article-new)
* [`mf article list`↴](#mf-article-list)
* [`mf article lint`↴](#mf-article-lint)
* [`mf article index`↴](#mf-article-index)
* [`mf article rename`↴](#mf-article-rename)
* [`mf article move`↴](#mf-article-move)
* [`mf article remove`↴](#mf-article-remove)
* [`mf article show`↴](#mf-article-show)
* [`mf article update`↴](#mf-article-update)
* [`mf article block`↴](#mf-article-block)
* [`mf article block new`↴](#mf-article-block-new)
* [`mf article block move`↴](#mf-article-block-move)
* [`mf article block renumber`↴](#mf-article-block-renumber)
* [`mf article block rename`↴](#mf-article-block-rename)
* [`mf article block rm`↴](#mf-article-block-rm)
* [`mf article convert`↴](#mf-article-convert)
* [`mf prompt`↴](#mf-prompt)
* [`mf prompt list`↴](#mf-prompt-list)
* [`mf prompt show`↴](#mf-prompt-show)
* [`mf thinking`↴](#mf-thinking)
* [`mf thinking list`↴](#mf-thinking-list)
* [`mf thinking show`↴](#mf-thinking-show)
* [`mf source`↴](#mf-source)
* [`mf source list`↴](#mf-source-list)
* [`mf source new`↴](#mf-source-new)
* [`mf source update`↴](#mf-source-update)
* [`mf source index`↴](#mf-source-index)
* [`mf source remove`↴](#mf-source-remove)
* [`mf source rename`↴](#mf-source-rename)
* [`mf source move`↴](#mf-source-move)
* [`mf source clean`↴](#mf-source-clean)
* [`mf source show`↴](#mf-source-show)
* [`mf source search`↴](#mf-source-search)
* [`mf source sync`↴](#mf-source-sync)
* [`mf source status`↴](#mf-source-status)
* [`mf source export`↴](#mf-source-export)
* [`mf source import`↴](#mf-source-import)
* [`mf source trace`↴](#mf-source-trace)
* [`mf source admin`↴](#mf-source-admin)
* [`mf source admin rebuild`↴](#mf-source-admin-rebuild)
* [`mf source admin clear`↴](#mf-source-admin-clear)
* [`mf source admin recover`↴](#mf-source-admin-recover)
* [`mf search`↴](#mf-search)
* [`mf asset`↴](#mf-asset)
* [`mf asset list`↴](#mf-asset-list)
* [`mf asset new`↴](#mf-asset-new)
* [`mf asset update`↴](#mf-asset-update)
* [`mf asset index`↴](#mf-asset-index)
* [`mf asset clean`↴](#mf-asset-clean)
* [`mf asset remove`↴](#mf-asset-remove)
* [`mf asset rename`↴](#mf-asset-rename)
* [`mf asset move`↴](#mf-asset-move)
* [`mf asset show`↴](#mf-asset-show)
* [`mf term`↴](#mf-term)
* [`mf term list`↴](#mf-term-list)
* [`mf term new`↴](#mf-term-new)
* [`mf term lint`↴](#mf-term-lint)
* [`mf term update`↴](#mf-term-update)
* [`mf term show`↴](#mf-term-show)
* [`mf term remove`↴](#mf-term-remove)
* [`mf term rename`↴](#mf-term-rename)
* [`mf term fix`↴](#mf-term-fix)
* [`mf term correction`↴](#mf-term-correction)
* [`mf term correction add`↴](#mf-term-correction-add)
* [`mf term correction list`↴](#mf-term-correction-list)
* [`mf term correction show`↴](#mf-term-correction-show)
* [`mf term correction update`↴](#mf-term-correction-update)
* [`mf term correction remove`↴](#mf-term-correction-remove)
* [`mf term move`↴](#mf-term-move)
* [`mf build`↴](#mf-build)
* [`mf publish`↴](#mf-publish)
* [`mf publish run`↴](#mf-publish-run)
* [`mf publish update`↴](#mf-publish-update)
* [`mf publish target`↴](#mf-publish-target)
* [`mf publish target list`↴](#mf-publish-target-list)
* [`mf publish target show`↴](#mf-publish-target-show)
* [`mf render`↴](#mf-render)
* [`mf render template`↴](#mf-render-template)
* [`mf render template list`↴](#mf-render-template-list)
* [`mf render template show`↴](#mf-render-template-show)
* [`mf config`↴](#mf-config)
* [`mf config schema`↴](#mf-config-schema)
* [`mf config show`↴](#mf-config-show)
* [`mf config generate`↴](#mf-config-generate)
* [`mf config default`↴](#mf-config-default)
* [`mf config terminal`↴](#mf-config-terminal)
* [`mf completion`↴](#mf-completion)
* [`mf version`↴](#mf-version)

## `mf`

mind-forge: a local-first knowledge management CLI

**Usage:** `mf [OPTIONS] [COMMAND]`

###### **Subcommands:**

* `init` — Initialize a directory as a Mind Repo
* `project` — Manage projects
* `article` — Manage articles
* `prompt` — View prompt projections
* `thinking` — View thinking projections
* `source` — Manage content sources
* `search` — Search the repository-wide RAG corpus
* `asset` — Manage project assets
* `term` — Manage terminology
* `build` — Build articles
* `publish` — Publish articles to configured targets
* `render` — Generate render prompts (emits prompts only, no output files)
* `config` — Manage configuration
* `completion` — Generate shell completion scripts
* `version` — Show version information

###### **Options:**

* `--root <PATH>` — Mind Repo root directory
* `--config <PATH>` — Config file path
* `-v`, `--verbose` — Verbose output
* `-q`, `--quiet` — Silence non-error output
* `-o`, `--output <FORMAT>` — Output format

  Default value: `text`

  Possible values: `text`, `json`

* `--json` — Shorthand for --output json
* `--no-color` — Disable colored output
* `-p`, `--project <NAME>` — Project name for project-scoped operations



## `mf init`

Initialize a directory as a Mind Repo

**Usage:** `mf init [PATH]`

###### **Arguments:**

* `<PATH>` — Target directory path (defaults to current directory)



## `mf project`

Manage projects

**Usage:** `mf project [COMMAND]`

###### **Subcommands:**

* `new` — Create a project
* `list` — List projects
* `archive` — Archive a project
* `lint` — Lint a project
* `index` — Index projects
* `show` — Show project details
* `update` — Update project metadata
* `import` — Import a directory as a project
* `rename` — Rename a project
* `remove` — Remove a project



## `mf project new`

Create a project

**Usage:** `mf project new [OPTIONS] <PATH>`

###### **Arguments:**

* `<PATH>` — Project path (cwd-relative, repo-relative, or simple name)

###### **Options:**

* `--template <TEMPLATE>`
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf project list`

List projects

**Usage:** `mf project list [OPTIONS]`

**Command Alias:** `ls`

###### **Options:**

* `--no-headers` — Suppress the table header row
* `--no-trunc` — Disable column truncation



## `mf project archive`

Archive a project

**Usage:** `mf project archive [OPTIONS] <NAME_OR_PATH>`

###### **Arguments:**

* `<NAME_OR_PATH>`

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `-y`, `--yes` — Skip interactive confirmation prompt
* `--dry-run` — Preview changes without writing



## `mf project lint`

Lint a project

**Usage:** `mf project lint [OPTIONS]`

###### **Options:**

* `--fix` — Auto-fix issues that have a fixer
* `--rule <RULE>` — Restrict to a specific lint rule kind
* `--severity <LEVEL>` — Only emit issues at or above this severity (error|warning|info)
* `--max-warnings <N>` — Exit 1 when warnings exceed this count
* `--dry-run` — Preview fixes without writing (only with --fix)
* `--include-suggested` — Apply all corrections including suggested
* `--min-confidence <0.0..1.0>` — Apply suggested corrections at or above this confidence



## `mf project index`

Index projects

**Usage:** `mf project index [OPTIONS]`

###### **Options:**

* `--dry-run` — Preview changes without writing



## `mf project show`

Show project details

**Usage:** `mf project show <PATH>`

###### **Arguments:**

* `<PATH>`



## `mf project update`

Update project metadata

**Usage:** `mf project update [OPTIONS] <PATH>`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `--description <DESCRIPTION>`
* `--clear-description`
* `--dry-run` — Preview changes without writing



## `mf project import`

Import a directory as a project

**Usage:** `mf project import [OPTIONS] <DIRECTORY>`

###### **Arguments:**

* `<DIRECTORY>`

###### **Options:**

* `--type <TYPE>`
* `--source <SOURCE>`
* `--assets <ASSETS>`
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `-y`, `--yes` — Skip interactive confirmation prompt



## `mf project rename`

Rename a project

**Usage:** `mf project rename [OPTIONS] <OLD_PATH> <NEW_PATH>`

###### **Arguments:**

* `<OLD_PATH>`
* `<NEW_PATH>`

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf project remove`

Remove a project

**Usage:** `mf project remove [OPTIONS] <PATH>`

**Command Alias:** `rm`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `-y`, `--yes` — Skip interactive confirmation prompt
* `--dry-run` — Preview changes without writing



## `mf article`

Manage articles

**Usage:** `mf article [COMMAND]`

###### **Subcommands:**

* `new` — Create an article
* `list` — List articles
* `lint` — Lint articles
* `index` — Index articles
* `rename` — Rename an article
* `move` — Move an article to another project
* `remove` — Remove an article
* `show` — Show article details
* `update` — Update article metadata
* `block` — Manage blocks within a directory article
* `convert` — Convert article shape between directory and single-file



## `mf article new`

Create an article

**Usage:** `mf article new [OPTIONS] <TITLE>`

###### **Arguments:**

* `<TITLE>` — Article title (sole positional)

###### **Options:**

* `--slug <SLUG>` — Optional ASCII-friendly filename slug. Without it the title is used
* `-t`, `--template <TEMPLATE>` — Template: built-in schema name (blank/arch/prd/blog) or path under project root. Default: blank

  Default value: `blank`
* `--file` [alias: `single-file`] — Write a single file instead of a directory

  Default value: `false`
* `--tag <TAG>`
* `--draft`

  Default value: `true`
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf article list`

List articles

**Usage:** `mf article list [OPTIONS]`

**Command Alias:** `ls`

###### **Options:**

* `--no-headers` — Suppress the table header row
* `--no-trunc` — Disable column truncation



## `mf article lint`

Lint articles

**Usage:** `mf article lint [OPTIONS]`

###### **Options:**

* `--fix` — Auto-fix issues that have a fixer
* `--rule <RULE>` — Restrict to a specific lint rule kind
* `--severity <LEVEL>` — Only emit issues at or above this severity (error|warning|info)
* `--max-warnings <N>` — Exit 1 when warnings exceed this count
* `--dry-run` — Preview fixes without writing (only with --fix)
* `--include-suggested` — Apply all corrections including suggested
* `--min-confidence <0.0..1.0>` — Apply suggested corrections at or above this confidence



## `mf article index`

Index articles

**Usage:** `mf article index [OPTIONS]`

###### **Options:**

* `--dry-run` — Preview changes without writing



## `mf article rename`

Rename an article

**Usage:** `mf article rename [OPTIONS] <OLD_PATH> <NEW_PATH>`

###### **Arguments:**

* `<OLD_PATH>` — Current article path or title
* `<NEW_PATH>` — New slug (e.g. "new-slug") — renames the file/directory, title is unchanged

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf article move`

Move an article to another project

**Usage:** `mf article move [OPTIONS] --to-project <TO_PROJECT> <PATH>`

###### **Arguments:**

* `<PATH>` — Article path or title

###### **Options:**

* `--to-project <TO_PROJECT>`
* `--dry-run` — Preview changes without writing



## `mf article remove`

Remove an article

**Usage:** `mf article remove [OPTIONS] <PATH>`

**Command Alias:** `rm`

###### **Arguments:**

* `<PATH>` — Article path (e.g. docs/weekly.md) or title

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `-y`, `--yes` — Skip interactive confirmation prompt
* `--dry-run` — Preview changes without writing



## `mf article show`

Show article details

**Usage:** `mf article show <PATH>`

###### **Arguments:**

* `<PATH>` — Article path (e.g. docs/weekly.md) or title



## `mf article update`

Update article metadata

**Usage:** `mf article update [OPTIONS] <PATH>`

###### **Arguments:**

* `<PATH>` — Article path (e.g. docs/weekly.md) or title

###### **Options:**

* `--status <STATUS>` — Article publication status
* `--title <TITLE>` — New article title (metadata only, does not rename files)
* `--dry-run` — Preview changes without writing



## `mf article block`

Manage blocks within a directory article

**Usage:** `mf article block <COMMAND>`

###### **Subcommands:**

* `new` — Create a block within a directory article
* `move` — Move a block within a directory article
* `renumber` — Renumber blocks contiguously
* `rename` — Rename a block within a directory article
* `rm` — Remove a block within a directory article



## `mf article block new`

Create a block within a directory article

**Usage:** `mf article block new [OPTIONS] <ARTICLE> <SLUG>`

###### **Arguments:**

* `<ARTICLE>`
* `<SLUG>` — New block slug without a numeric prefix

###### **Options:**

* `--after <AFTER>`
* `--start <START>`

  Default value: `1`
* `--dry-run` — Preview changes without writing



## `mf article block move`

Move a block within a directory article

**Usage:** `mf article block move [OPTIONS] <ARTICLE> <BLOCK>`

###### **Arguments:**

* `<ARTICLE>`
* `<BLOCK>`

###### **Options:**

* `--after <AFTER>`
* `--start <START>`

  Default value: `1`
* `--dry-run` — Preview changes without writing



## `mf article block renumber`

Renumber blocks contiguously

**Usage:** `mf article block renumber [OPTIONS] <ARTICLE>`

###### **Arguments:**

* `<ARTICLE>`

###### **Options:**

* `--start <START>`

  Default value: `1`
* `--dry-run` — Preview changes without writing



## `mf article block rename`

Rename a block within a directory article

**Usage:** `mf article block rename [OPTIONS] <ARTICLE> <OLD_BLOCK> <NEW_SLUG>`

###### **Arguments:**

* `<ARTICLE>` — Article path (e.g. docs/my-article) or title
* `<OLD_BLOCK>` — Current block filename (e.g. "02-notes.md") or slug (e.g. "notes")
* `<NEW_SLUG>` — New slug — the number prefix is preserved (e.g. "thoughts" → "02-thoughts.md")

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf article block rm`

Remove a block within a directory article

**Usage:** `mf article block rm [OPTIONS] <ARTICLE> <BLOCK>`

**Command Alias:** `remove`

###### **Arguments:**

* `<ARTICLE>` — Article path (e.g. docs/my-article) or title
* `<BLOCK>` — Block filename (e.g. "02-notes.md"), stem (e.g. "02-notes"), or slug (e.g. "notes")

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `-y`, `--yes` — Skip interactive confirmation prompt
* `--dry-run` — Preview changes without writing



## `mf article convert`

Convert article shape between directory and single-file

**Usage:** `mf article convert [OPTIONS]`

###### **Options:**

* `--to-single-file` — Convert directory articles to single-file articles
* `--to-directory` — Convert single-file articles to directory articles
* `--merge` — Allow `--to-single-file` to merge multi-block directory articles (concatenated in filename order) instead of skipping them
* `--dry-run` — Preview changes without writing



## `mf prompt`

View prompt projections

**Usage:** `mf prompt [COMMAND]`

###### **Subcommands:**

* `list` —
* `show` —



## `mf prompt list`

**Usage:** `mf prompt list`



## `mf prompt show`

**Usage:** `mf prompt show <PATH>`

###### **Arguments:**

* `<PATH>`



## `mf thinking`

View thinking projections

**Usage:** `mf thinking [COMMAND]`

###### **Subcommands:**

* `list` —
* `show` —



## `mf thinking list`

**Usage:** `mf thinking list`



## `mf thinking show`

**Usage:** `mf thinking show <PATH>`

###### **Arguments:**

* `<PATH>`



## `mf source`

Manage content sources

**Usage:** `mf source [COMMAND]`

###### **Subcommands:**

* `list` — List sources
* `new` — Create a source
* `update` — Update a source
* `index` — Index sources
* `remove` — Remove a source
* `rename` — Rename a source
* `move` — Move a source to another project
* `clean` — Clean source index
* `show` — Show source details
* `search` — Search sources across the repository
* `sync` — Synchronize sources and article content
* `status` — Report source corpus status
* `export` — Export the source corpus
* `import` — Import a source corpus bundle
* `trace` — Trace source locations as Markdown links
* `admin` — Maintain the source corpus



## `mf source list`

List sources

**Usage:** `mf source list [OPTIONS]`

**Command Alias:** `ls`

###### **Options:**

* `--filter <FILTER>`
* `-t`, `--type <KIND>`

  Possible values: `auto`, `pdf`, `file`, `rss`, `web`

* `--no-headers` — Suppress the table header row
* `--no-trunc` — Disable column truncation



## `mf source new`

Create a source

**Usage:** `mf source new [OPTIONS] <INPUT>`

###### **Arguments:**

* `<INPUT>`

###### **Options:**

* `-n`, `--name <NAME>` — Override the source name derived from the input
* `--file-kind <FILE_KIND>` — File kind (mf primary). Use --source-kind for mind channel type

  Possible values: `auto`, `pdf`, `file`, `rss`, `web`

* `--source-kind <SOURCE_KIND>` — Source channel type (mind primary)

  Possible values: `yuque`, `meeting`, `misc`

* `--link` — Create a symlink instead of copying a local file
* `--register-only` — Register a file already inside the project's sources directory without copying it
* `--no-index` — Register only, without indexing the source into RAG (Lance backend). By default a new source is chunked and embedded so it is searchable at once
* `--article <ARTICLE>` — Originating article that introduced this source (project-relative path). Captured as authoritative import provenance (spec 071). Must stay within the project; an escaping path is a usage error
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf source update`

Update a source

**Usage:** `mf source update [OPTIONS] <PATH>`

###### **Arguments:**

* `<PATH>` — Source path (e.g. sources/meeting/notes.md) or name

###### **Options:**

* `--rename <RENAME>`
* `--url <URL>`
* `--dry-run` — Preview changes without writing



## `mf source index`

Index sources

**Usage:** `mf source index [OPTIONS]`

###### **Options:**

* `--dry-run` — Preview changes without writing



## `mf source remove`

Remove a source

**Usage:** `mf source remove [OPTIONS] <NAME_OR_PATH>`

**Command Alias:** `rm`

###### **Arguments:**

* `<NAME_OR_PATH>` — Source path (e.g. sources/yuque/foo.md) or source name (deprecated)

###### **Options:**

* `--keep-file`
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `-y`, `--yes` — Skip interactive confirmation prompt
* `--dry-run` — Preview changes without writing



## `mf source rename`

Rename a source

**Usage:** `mf source rename [OPTIONS] <OLD_PATH> <NEW_PATH>`

###### **Arguments:**

* `<OLD_PATH>` — Current source path or name
* `<NEW_PATH>` — New source path or name

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf source move`

Move a source to another project

**Usage:** `mf source move [OPTIONS] --to-project <TO_PROJECT> <PATH>`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `--to-project <TO_PROJECT>`
* `--dry-run` — Preview changes without writing



## `mf source clean`

Clean source index

**Usage:** `mf source clean [OPTIONS]`

###### **Options:**

* `--dry-run` — Preview changes without writing



## `mf source show`

Show source details

**Usage:** `mf source show <PATH>`

###### **Arguments:**

* `<PATH>` — Source path (e.g. sources/meeting/notes.md) or name



## `mf source search`

Search sources across the repository

**Usage:** `mf source search [OPTIONS] <QUERY>`

###### **Arguments:**

* `<QUERY>` — Search query

###### **Options:**

* `--mode <MODE>` — Search mode: basic (metadata), advanced (content), or both (fused)

  Possible values: `basic`, `advanced`, `both`

* `-p`, `--project <PROJECT>` — Limit search to a specific project
* `--file-kind <FILE_KIND>` — Filter by file kind
* `--source <SOURCE>` — Filter by source identity
* `--label <KEY=VALUE>` — Filter by label, `key=value` (repeatable; all must match)
* `--revision <REV>` — Search a specific content revision instead of the current version. Accepts an integer revision number, a date (2026-07-25), or a relative date (yesterday, "7 days ago")
* `--limit <LIMIT>` — Maximum results to return

  Default value: `20`



## `mf source sync`

Synchronize sources and article content

**Usage:** `mf source sync [OPTIONS]`

###### **Options:**

* `-p`, `--project <PROJECT>` — Limit sync to a specific project
* `--source <SOURCE>` — Limit sync to a specific Source identity (requires unambiguous scope)
* `--offline` — Forbid all network access (Web/RSS acquisition disabled)
* `--rebuild` — Regenerate the Lance Source index to the current storage schema (full re-index/re-embed). Recovers from a schema-drift refusal in one command family instead of detouring to `source admin rebuild`
* `--dry-run` — Preview changes without writing



## `mf source status`

Report source corpus status

**Usage:** `mf source status [OPTIONS]`

###### **Options:**

* `-p`, `--project <PROJECT>`
* `--source <SOURCE>`



## `mf source export`

Export the source corpus

**Usage:** `mf source export [OPTIONS] --output-dir <DIR>`

###### **Options:**

* `--output-dir <DIR>` — Destination bundle directory
* `--force` — Allow writing into an existing/non-empty output directory
* `--dry-run` — Preview changes without writing



## `mf source import`

Import a source corpus bundle

**Usage:** `mf source import [OPTIONS] <BUNDLE>`

###### **Arguments:**

* `<BUNDLE>` — Bundle directory produced by `export`

###### **Options:**

* `--overwrite` — Required when the target already has a published corpus
* `--dry-run` — Preview changes without writing



## `mf source trace`

Trace source locations as Markdown links

**Usage:** `mf source trace [OPTIONS]`

###### **Options:**

* `-p`, `--project <PROJECT>` — Limit to one project



## `mf source admin`

Maintain the source corpus

**Usage:** `mf source admin <COMMAND>`

###### **Subcommands:**

* `rebuild` —
* `clear` —
* `recover` —



## `mf source admin rebuild`

**Usage:** `mf source admin rebuild [OPTIONS]`

###### **Options:**

* `--offline`
* `--dry-run` — Preview changes without writing



## `mf source admin clear`

**Usage:** `mf source admin clear [OPTIONS] [SOURCE]`

###### **Arguments:**

* `<SOURCE>`

###### **Options:**

* `-p`, `--project <PROJECT>`
* `--all`
* `--dry-run` — Preview changes without writing
* `-y`, `--yes` — Skip interactive confirmation prompt



## `mf source admin recover`

**Usage:** `mf source admin recover [OPTIONS] --snapshot <SNAPSHOT>`

###### **Options:**

* `--snapshot <SNAPSHOT>`
* `--dry-run` — Preview changes without writing
* `-y`, `--yes` — Skip interactive confirmation prompt



## `mf search`

Search the repository-wide RAG corpus

**Usage:** `mf search [OPTIONS] <QUERY>`

###### **Arguments:**

* `<QUERY>`

###### **Options:**

* `-p`, `--project <PROJECT>`
* `--file-kind <FILE_KIND>`
* `--source <SOURCE>`
* `--label <KEY=VALUE>`
* `--revision <REV>`
* `--limit <LIMIT>`

  Default value: `20`



## `mf asset`

Manage project assets

**Usage:** `mf asset [COMMAND]`

###### **Subcommands:**

* `list` — List assets
* `new` — Create an asset
* `update` — Update assets
* `index` — Index assets
* `clean` — Clean stale asset index entries
* `remove` — Remove an asset
* `rename` — Rename an asset
* `move` — Move an asset to another project
* `show` — Show asset details



## `mf asset list`

List assets

**Usage:** `mf asset list [OPTIONS]`

**Command Alias:** `ls`

###### **Options:**

* `--filter <FILTER>`
* `--type <ASSET_TYPE>`

  Possible values: `image`, `video`, `audio`, `other`

* `--no-headers` — Suppress the table header row
* `--no-trunc` — Disable column truncation



## `mf asset new`

Create an asset

**Usage:** `mf asset new [OPTIONS] <PATH>`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `--name <NAME>`
* `--tag <TAG>`
* `--copy`
* `--link`
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf asset update`

Update assets

**Usage:** `mf asset update [OPTIONS] [PATH]`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `--set-url <SET_URL>`
* `--channel <CHANNEL>`
* `--all`
* `--dry-run` — Preview changes without writing



## `mf asset index`

Index assets

**Usage:** `mf asset index [OPTIONS]`

###### **Options:**

* `--dry-run` — Preview changes without writing
* `--refresh-metadata`



## `mf asset clean`

Clean stale asset index entries

**Usage:** `mf asset clean [OPTIONS]`

###### **Options:**

* `--dry-run` — Preview changes without writing



## `mf asset remove`

Remove an asset

**Usage:** `mf asset remove [OPTIONS] <PATH>`

**Command Alias:** `rm`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `-y`, `--yes` — Skip interactive confirmation prompt
* `--dry-run` — Preview changes without writing



## `mf asset rename`

Rename an asset

**Usage:** `mf asset rename [OPTIONS] <OLD_PATH> <NEW_PATH>`

###### **Arguments:**

* `<OLD_PATH>`
* `<NEW_PATH>`

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf asset move`

Move an asset to another project

**Usage:** `mf asset move [OPTIONS] --to-project <TO_PROJECT> <PATH>`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `--to-project <TO_PROJECT>`
* `--dry-run` — Preview changes without writing



## `mf asset show`

Show asset details

**Usage:** `mf asset show <PATH>`

###### **Arguments:**

* `<PATH>` — Asset path (e.g. assets/chart.png) or name



## `mf term`

Manage terminology

**Usage:** `mf term [COMMAND]`

**Command Alias:** `terms`

###### **Subcommands:**

* `list` — List terms
* `new` — Create a term
* `lint` — Lint term consistency in project docs
* `update` — Update term metadata
* `show` — Show term details
* `remove` — Remove a term
* `rename` — Rename a term
* `fix` — Apply term corrections to documents (alias of `term lint --fix`)
* `correction` — Manage term corrections
* `move` — Move a term between scopes



## `mf term list`

List terms

**Usage:** `mf term list [OPTIONS]`

**Command Alias:** `ls`

###### **Options:**

* `--filter <FILTER>` — Match the canonical term name (substring)
* `--tag <TAG>` — Match the term's tag field
* `--alias <ALIAS>` — Match the term's alias field (does not match the term name)
* `--has-correction` — Only show terms that have at least one correction
* `--scope <SCOPE>` — Filter by scope: project, global, or all (default: project + global fallback)
* `--no-headers` — Suppress the table header row
* `--no-trunc` — Disable column truncation



## `mf term new`

Create a term

**Usage:** `mf term new [OPTIONS] [TERM]`

###### **Arguments:**

* `<TERM>` — Canonical term name

###### **Options:**

* `--definition <DEFINITION>`
* `--description <DESCRIPTION>`
* `--confidence <CONFIDENCE>`
* `--alias <ALIAS>`
* `--tag <TAG>`
* `--misrecognition <MISRECOGNITION>`
* `--dry-run` — Preview changes without writing



## `mf term lint`

Lint term consistency in project docs

**Usage:** `mf term lint [OPTIONS] [PATH]`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `--article <ARTICLE>` — Target a specific article by slug or title
* `--term <NAME>` — Only apply/scan corrections for the named term(s) (repeatable, exact name)
* `--exclude-term <NAME>` — Exclude corrections for the named term(s) (repeatable)
* `--exclude-original <ORIGINAL>` — Exclude this exact original text across terms (repeatable)
* `--include-quotes` — Scan quoted text and blockquotes as well as ordinary prose
* `--ad-hoc <ORIGINAL=>CORRECT>` — Apply an invocation-only correction (repeatable): ORIGINAL=>CORRECT
* `--ad-hoc-from <PATH>` — Read invocation-only corrections, one ORIGINAL=>CORRECT per line
* `--fix` — Auto-fix issues that have a fixer
* `--rule <RULE>` — Restrict to a specific lint rule kind
* `--severity <LEVEL>` — Only emit issues at or above this severity (error|warning|info)
* `--max-warnings <N>` — Exit 1 when warnings exceed this count
* `--dry-run` — Preview fixes without writing (only with --fix)
* `--include-suggested` — Apply all corrections including suggested
* `--min-confidence <0.0..1.0>` — Apply suggested corrections at or above this confidence
* `-y`, `--yes` — Skip interactive confirmation prompt



## `mf term update`

Update term metadata

**Usage:** `mf term update [OPTIONS] <TERM>`

###### **Arguments:**

* `<TERM>`

###### **Options:**

* `--definition <DEFINITION>`
* `--description <DESCRIPTION>`
* `--clear-description`
* `--confidence <CONFIDENCE>`
* `--clear-confidence`
* `--alias <ALIAS>`
* `--tag <TAG>`
* `--delete-alias <DELETE_ALIAS>` — Remove an alias from the term
* `--delete-tag <DELETE_TAG>` — Remove a tag from the term
* `--add-correction <ORIGINAL[:CORRECT]>` — Append a correction (ORIGINAL[:CORRECT]); defaults to word/required, term name as CORRECT
* `--correction-match <ORIGINAL:KIND>` — Set match kind of a correction: ORIGINAL:word|substring|pinyin
* `--correction-fix <ORIGINAL:KIND>` — Set fix kind of a correction: ORIGINAL:required|suggested
* `--correction-pinyin <ORIGINAL:PINYIN>` — Set pinyin of a correction: ORIGINAL:PINYIN
* `--delete-correction <DELETE_CORRECTION>` — Delete a correction by ORIGINAL
* `--dry-run` — Preview changes without writing



## `mf term show`

Show term details

**Usage:** `mf term show <TERM>`

###### **Arguments:**

* `<TERM>`



## `mf term remove`

Remove a term

**Usage:** `mf term remove [OPTIONS] <TERM>`

**Command Alias:** `rm`

###### **Arguments:**

* `<TERM>`

###### **Options:**

* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `-y`, `--yes` — Skip interactive confirmation prompt
* `--dry-run` — Preview changes without writing



## `mf term rename`

Rename a term

**Usage:** `mf term rename [OPTIONS] <OLD_TERM> <NEW_TERM>`

###### **Arguments:**

* `<OLD_TERM>`
* `<NEW_TERM>`

###### **Options:**

* `--keep-alias`
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf term fix`

Apply term corrections to documents (alias of `term lint --fix`)

**Usage:** `mf term fix [OPTIONS] [PATH]`

###### **Arguments:**

* `<PATH>`

###### **Options:**

* `--article <ARTICLE>` — Target a specific article by slug or title
* `--term <NAME>` — Only apply/scan corrections for the named term(s) (repeatable, exact name)
* `--exclude-term <NAME>` — Exclude corrections for the named term(s) (repeatable)
* `--exclude-original <ORIGINAL>` — Exclude this exact original text across terms (repeatable)
* `--include-quotes` — Scan quoted text and blockquotes as well as ordinary prose
* `--ad-hoc <ORIGINAL=>CORRECT>` — Apply an invocation-only correction (repeatable): ORIGINAL=>CORRECT
* `--ad-hoc-from <PATH>` — Read invocation-only corrections, one ORIGINAL=>CORRECT per line
* `--fix` — Auto-fix issues that have a fixer
* `--rule <RULE>` — Restrict to a specific lint rule kind
* `--severity <LEVEL>` — Only emit issues at or above this severity (error|warning|info)
* `--max-warnings <N>` — Exit 1 when warnings exceed this count
* `--dry-run` — Preview fixes without writing (only with --fix)
* `--include-suggested` — Apply all corrections including suggested
* `--min-confidence <0.0..1.0>` — Apply suggested corrections at or above this confidence
* `-y`, `--yes` — Skip interactive confirmation prompt



## `mf term correction`

Manage term corrections

**Usage:** `mf term correction <COMMAND>`

###### **Subcommands:**

* `add` — Add a correction to a term
* `list` — List corrections for a term
* `show` — Show a single correction
* `update` — Update correction attributes
* `remove` — Remove a correction from a term



## `mf term correction add`

Add a correction to a term

**Usage:** `mf term correction add [OPTIONS] <TERM> <ORIGINAL> <CORRECT>`

###### **Arguments:**

* `<TERM>`
* `<ORIGINAL>`
* `<CORRECT>`

###### **Options:**

* `--match <MATCH>`
* `--fix <FIX>`
* `--pinyin <PINYIN>`
* `--boundary <BOUNDARY>`
* `--dry-run` — Preview changes without writing



## `mf term correction list`

List corrections for a term

**Usage:** `mf term correction list [OPTIONS] <TERM>`

###### **Arguments:**

* `<TERM>`

###### **Options:**

* `--no-headers` — Suppress the table header row
* `--no-trunc` — Disable column truncation



## `mf term correction show`

Show a single correction

**Usage:** `mf term correction show <TERM> <ORIGINAL>`

###### **Arguments:**

* `<TERM>`
* `<ORIGINAL>`



## `mf term correction update`

Update correction attributes

**Usage:** `mf term correction update [OPTIONS] <TERM> <ORIGINAL>`

###### **Arguments:**

* `<TERM>`
* `<ORIGINAL>`

###### **Options:**

* `--correct <CORRECT>`
* `--match <MATCH>`
* `--fix <FIX>`
* `--pinyin <PINYIN>`
* `--boundary <BOUNDARY>`
* `--dry-run` — Preview changes without writing



## `mf term correction remove`

Remove a correction from a term

**Usage:** `mf term correction remove [OPTIONS] <TERM> <ORIGINAL>`

###### **Arguments:**

* `<TERM>`
* `<ORIGINAL>`

###### **Options:**

* `--dry-run` — Preview changes without writing



## `mf term move`

Move a term between scopes

**Usage:** `mf term move [OPTIONS] <TERM>`

**Command Alias:** `mv`

###### **Arguments:**

* `<TERM>`

###### **Options:**

* `--to-project <TO_PROJECT>` — Destination project for the term
* `--to-global` — Move to global scope
* `--from-global` — Source is global scope (default: project-scoped via -p)
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others
* `--dry-run` — Preview changes without writing



## `mf build`

Build articles

**Usage:** `mf build [OPTIONS] <ARTICLE>`

###### **Arguments:**

* `<ARTICLE>`

###### **Options:**

* `--out <OUTPUT>`
* `--dry-run` — Preview changes without writing



## `mf publish`

Publish articles to configured targets

**Usage:** `mf publish [COMMAND]`

###### **Subcommands:**

* `run` — Publish an article to a target
* `update` — Update publish record metadata
* `target` — List and manage publish targets



## `mf publish run`

Publish an article to a target

**Usage:** `mf publish run [OPTIONS] <ARTICLE>`

###### **Arguments:**

* `<ARTICLE>` — Article name (kebab-case, no extension, no path separators)

###### **Options:**

* `--target <TARGET>`
* `--dry-run` — Preview changes without writing
* `-f`, `--force` — Proceed despite safety checks: overwrite an existing target, or remove an entity referenced by others



## `mf publish update`

Update publish record metadata

**Usage:** `mf publish update [OPTIONS] --target <TARGET> <ARTICLE>`

###### **Arguments:**

* `<ARTICLE>` — Article name (kebab-case, no extension, no path separators)

###### **Options:**

* `--target <TARGET>`
* `--status <STATUS>`

  Possible values: `draft`, `published`, `archived`

* `--target-url <TARGET_URL>`
* `--set <KEY=VALUE>`
* `--dry-run` — Preview changes without writing



## `mf publish target`

List and manage publish targets

**Usage:** `mf publish target [COMMAND]`

###### **Subcommands:**

* `list` — List publishers and diagnostics
* `show` — Show publish target details



## `mf publish target list`

List publishers and diagnostics

**Usage:** `mf publish target list [OPTIONS]`

###### **Options:**

* `--no-headers` — Suppress the table header row
* `--no-trunc` — Disable column truncation



## `mf publish target show`

Show publish target details

**Usage:** `mf publish target show <NAME>`

###### **Arguments:**

* `<NAME>`



## `mf render`

Generate render prompts (emits prompts only, no output files)

**Usage:** `mf render [OPTIONS] [ARTICLE] [COMMAND]`

###### **Subcommands:**

* `template` — Manage render templates

###### **Arguments:**

* `<ARTICLE>` — Article name to render

###### **Options:**

* `--template <TEMPLATE>` — Render template name
* `--html-form <HTML_FORM>` — HTML output form (document or fragment)

  Possible values:
  - `document`:
    Full HTML document with `<html>`, `<head>`, and `<body>`
  - `fragment`:
    Embeddable HTML fragment without outer document structure




## `mf render template`

Manage render templates

**Usage:** `mf render template <COMMAND>`

###### **Subcommands:**

* `list` — List available render templates
* `show` — Show template details



## `mf render template list`

List available render templates

**Usage:** `mf render template list [OPTIONS]`

###### **Options:**

* `--no-headers` — Suppress the table header row
* `--no-trunc` — Disable column truncation



## `mf render template show`

Show template details

**Usage:** `mf render template show <NAME>`

###### **Arguments:**

* `<NAME>`



## `mf config`

Manage configuration

**Usage:** `mf config [COMMAND]`

###### **Subcommands:**

* `schema` — Show config schema
* `show` — Show effective config
* `generate` — Generate effective config file
* `default` — Show default config values
* `terminal` — Show terminal capability diagnostics



## `mf config schema`

Show config schema

**Usage:** `mf config schema [OPTIONS]`

###### **Options:**

* `--output-format <OUTPUT_FORMAT>`

  Default value: `json`



## `mf config show`

Show effective config

**Usage:** `mf config show [OPTIONS]`

###### **Options:**

* `--output-format <OUTPUT_FORMAT>`

  Default value: `yaml`



## `mf config generate`

Generate effective config file

**Usage:** `mf config generate [OPTIONS] --out <OUTPUT>`

###### **Options:**

* `--output-format <OUTPUT_FORMAT>`

  Default value: `yaml`
* `--out <OUTPUT>`



## `mf config default`

Show default config values

**Usage:** `mf config default [OPTIONS]`

###### **Options:**

* `--output-format <OUTPUT_FORMAT>`

  Default value: `yaml`



## `mf config terminal`

Show terminal capability diagnostics

**Usage:** `mf config terminal`



## `mf completion`

Generate shell completion scripts

**Usage:** `mf completion <SHELL>`

###### **Arguments:**

* `<SHELL>`

  Possible values: `bash`, `zsh`, `fish`, `powershell`, `elvish`




## `mf version`

Show version information

**Usage:** `mf version`



<hr/>

<small><i>
    This document was generated automatically by
    <a href="https://crates.io/crates/clap-markdown"><code>clap-markdown</code></a>.
</i></small>
